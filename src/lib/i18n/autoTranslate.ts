// Legacy auto-translator removed.
// We now use i18next with explicit JSON keys (see `src/i18n/`).
// This file remains as a no-op for backwards compatibility.

export type Locale = "pt" | "en" | "es" | "pt-PT" | "pt-BR";

export function applyLocale(_locale: Locale) {
  // Intentional no-op. All translation is handled by `react-i18next`.
}
